# Post-verification state: the fake key that previously lived here was caught
# by the gitleaks gate (and the branch history rewritten, since a revert
# leaves the credential live in history). No static credentials exist in this
# repository — CI authenticates via OIDC federation.
